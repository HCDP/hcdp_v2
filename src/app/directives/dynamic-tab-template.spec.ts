import { DynamicTabTemplate } from './dynamic-tab-template';

describe('DynamicTabTemplate', () => {
  it('should create an instance', () => {
    const directive = new DynamicTabTemplate();
    expect(directive).toBeTruthy();
  });
});
